# 工厂安全数字孪生系统介绍

## 1. 系统定位

本项目是一套面向工业安全生产场景的 3D 数字孪生与智能体协同系统。系统围绕“真实感知、智能分析、可信决策、人工审批、执行回写、三维展示”构建完整闭环，目标不是单纯做一个可视化大屏，而是形成一套具备工程可解释性和比赛展示说服力的安全监控平台原型。

当前系统的核心能力包括：

- 接入真实海康摄像头 RTSP 视频流。
- 接收边缘检测盒子或演示回放产生的目标检测结果。
- 将像素域检测框映射到 3D 数字孪生场景。
- 基于规则判断未戴安全帽、未穿反光背心、区域入侵、人车混行、火焰等事件。
- 调用本地 Qwen2.5-VL 多模态大模型进行视觉语义分析。
- 基于规则等级和 LLM 建议进行调度裁决。
- 对 A 类高危事件执行 Human-in-the-Loop 可信审批。
- 审批通过后生成执行回写，模拟声光报警、设备联锁、现场工单派发。
- 通过 WebSocket 将检测、分析、裁决、审批、执行结果实时推送到前端。
- 前端以 Three.js 3D 工厂场景、可信闭环、告警卡片、证据截图、审批按钮、实时视频等形式展示系统状态。
- 提供演示回放模式，现场可稳定触发 A/B/C 类事件，避免比赛时依赖真实危险行为。

系统可以概括为：

```text
摄像头/边缘检测
    ↓
感知 Agent
    ↓
安全 Agent + 多模态大模型 + 记忆检索
    ↓
调度 Agent
    ↓
数据库 / 通知 / 报告 / 人工审批 / 执行回写
    ↓
WebSocket
    ↓
3D 数字孪生前端
```

## 2. 硬件与运行环境

### 2.1 物理设备

| 设备 | 作用 | 当前用途 |
|---|---|---|
| 海康摄像头 | 视频采集 | 提供 RTSP 实时视频流 |
| 海康边缘盒子 | 边缘检测 | 可运行目标检测模型并向后端推送报警 |
| Windows 开发主机 | 中枢智能和前端展示 | 运行 Python 后端、Ollama、Three.js 前端 |
| 本地/公网通知通道 | 告警通知 | 支持钉钉/企微 Webhook |

### 2.2 典型网络拓扑

```text
摄像头 10.53.4.81
    │ RTSP 554
    ↓
边缘盒子 10.53.4.82
    │ HTTP报警 / 推理结果
    ↓
开发PC
    ├── 5000  后端HTTP API
    ├── 5001  WebSocket推送
    ├── 8080  前端静态页面
    └── 11434 Ollama/Qwen2.5-VL
```

### 2.3 当前主要端口

| 端口 | 服务 | 说明 |
|---|---|---|
| 554 | 摄像头 RTSP | 真实视频源 |
| 5000 | Python 后端 HTTP | 报警接收、审批、健康检查、视频流转发 |
| 5001 | WebSocket | 前端实时事件推送 |
| 8080 | 前端静态服务 | 3D 数字孪生页面 |
| 11434 | Ollama | 本地 Qwen2.5-VL 推理接口 |

## 3. 工程目录

当前工程已经做过解耦，核心目录如下：

```text
digital_twin/
├── backend.py
├── serve.py
├── setup_box.py
├── README.md
├── 系统介绍.md
│
├── agents/
│   ├── __init__.py
│   ├── perception.py
│   ├── safety_agent.py
│   ├── dispatch.py
│   └── memory.py
│
├── tools/
│   ├── __init__.py
│   ├── database.py
│   ├── human_loop.py
│   ├── notifier.py
│   ├── reporter.py
│   └── actuator.py
│
├── services/
│   ├── __init__.py
│   ├── camera_stream.py
│   ├── recent_events.py
│   └── demo_scenarios.py
│
├── frontend/
│   ├── index.html
│   ├── css/
│   │   └── twin.css
│   ├── js/
│   │   └── app.js
│   ├── low_poly_industrial_zone.glb
│   ├── construction_worker_in_safety_gear.glb
│   └── construction_worker_in_high-visibility_vest.glb
│
├── data/
│   ├── alarms.db
│   ├── pending/
│   ├── reports/
│   └── executions/
│
└── alarms/
```

## 4. 后端总体结构

### 4.1 backend.py

`backend.py` 是系统编排入口，主要负责：

- 初始化 Agent。
- 初始化工具层。
- 初始化服务层。
- 提供 HTTP API。
- 接收 `/alarm` 报警。
- 启动 Agent 异步分析管线。
- 处理前端审批请求。
- 调用执行器进行执行回写。
- 向 WebSocket 队列广播事件。

它现在不再直接承担所有细节，已经把摄像头、最近事件缓存、演示样例拆到 `services/`。

### 4.2 services 层

`services/` 是后端基础服务层，负责非 Agent 的通用能力。

| 文件 | 职责 |
|---|---|
| `camera_stream.py` | 拉取 RTSP 视频流，编码 JPEG，提供 MJPEG 所需的最新帧和健康状态 |
| `recent_events.py` | 缓存最近事件，按 `event_id` 合并告警、LLM分析、审批、执行回写 |
| `demo_scenarios.py` | 生成 A/B/C 演示样例和演示证据图 |

拆分后的好处：

- `backend.py` 更像编排层。
- 摄像头逻辑可以单独维护。
- 演示样例不会污染真实报警处理逻辑。
- 最近事件缓存可以复用和测试。

### 4.3 agents 层

`agents/` 是多智能体系统核心。

| Agent | 文件 | 职责 |
|---|---|---|
| 感知 Agent | `perception.py` | 将目标检测框转换成安全事件 |
| 安全 Agent | `safety_agent.py` | 调用记忆检索和 Qwen2.5-VL 进行视觉分析 |
| 调度 Agent | `dispatch.py` | 规则 + LLM 建议裁决，调用工具 |
| 记忆模块 | `memory.py` | 检索历史事件，提供空间区域上下文 |

### 4.4 tools 层

`tools/` 是 Agent 能调用的工具集合。

| 工具 | 文件 | 作用 |
|---|---|---|
| 数据库工具 | `database.py` | 保存事件、LLM建议、调度结果、审批状态、执行状态 |
| 人工审批工具 | `human_loop.py` | 生成审批工单、审批通过、审批驳回 |
| 通知工具 | `notifier.py` | 钉钉/企微消息推送 |
| 报告工具 | `reporter.py` | 事件日志和 Markdown 报告 |
| 执行器工具 | `actuator.py` | 审批后执行回写，模拟联锁、声光报警、派单 |

## 5. 数据流详解

### 5.1 真实报警链路

```text
边缘盒子 / 外部检测程序
    ↓
POST /alarm
    ↓
backend.py 解析 JSON 和截图
    ↓
PerceptionAgent.process()
    ↓
生成 AlarmEvent
    ↓
基础告警先通过 WebSocket 推给前端
    ↓
SafetyAgent.analyze()
    ├── MemoryModule 检索历史
    └── Qwen2.5-VL 视觉分析
    ↓
DispatchAgent.dispatch()
    ├── 规则等级
    ├── LLM建议等级
    └── 最终调度裁决
    ↓
工具调用
    ├── human_loop
    ├── database
    ├── notifier
    └── reporter
    ↓
WebSocket 推送 alarm_with_llm
    ↓
前端更新 3D 场景、可信链路、AI分析、告警卡片
```

### 5.2 A 类高危闭环

```text
A类事件
    ↓
human_loop.check
    ↓
生成 PENDING_xxx 审批工单
    ↓
前端显示审批卡片
    ↓
安全员点击 授权执行 / 驳回指令
    ↓
POST /approval/approve 或 /approval/reject
    ↓
后端校验工单状态
    ↓
human_loop 更新工单
    ↓
database 更新 approval_status
    ↓
actuator 执行或取消处置
    ↓
database 更新 execution_status
    ↓
WebSocket 广播 approval_result
    ↓
前端显示审批结果和执行回写
```

前端可信闭环显示为：

```text
感知 → 认知 → 裁决 → 工具 → 审批 → 执行
```

### 5.3 演示回放链路

演示回放不绕过系统，而是通过 `/demo/trigger` 生成模拟检测输入，再进入真实 Agent 管线。

```text
前端点击 A/B/C 演示按钮
    ↓
POST /demo/trigger
    ↓
services/demo_scenarios.py 生成 objInfo + 证据图
    ↓
PerceptionAgent
    ↓
SafetyAgent
    ↓
DispatchAgent
    ↓
审批/存储/通知/报告/执行
    ↓
WebSocket 推回前端
```

这保证现场演示具有稳定性，同时流程仍然是真实系统流程。

## 6. 智能体模块详解

### 6.1 AlarmEvent

`AlarmEvent` 是多智能体之间传递的标准数据结构，定义在 `agents/__init__.py`。

它承载：

- 事件时间。
- 结构化安全事件列表。
- 原始 JSON。
- 图片字节。
- 图片 URL。
- LLM 分析文本。
- LLM 结构化建议。
- 调度裁决。
- 工具执行结果。
- 审批 ID。
- 审批状态。
- 生命周期状态。
- 时间线。

标准化事件对象让不同 Agent 不直接依赖原始盒子格式，降低耦合。

### 6.2 感知 Agent

感知 Agent 负责把检测目标变成安全事件。

支持目标类型：

| targetType | 含义 |
|---|---|
| 0 | 人员 |
| 1 | 安全帽 |
| 2 | 反光背心 |
| 3 | 火焰 |
| 4 | 车辆 |
| 5 | 头部 |

主要判断逻辑：

- 安全帽判断：安全帽框中心是否落在人体上半身区域。
- 反光背心判断：反光背心框与人体框是否有足够重叠。
- 区域入侵判断：人员中心点是否进入预定义危险区域。
- 人车混行判断：人员中心点与车辆中心点距离是否低于阈值。
- 火焰判断：检测到火焰目标即生成 A 类事件。
- 复合风险判断：多种违规叠加时可升级为高危。

### 6.3 安全 Agent

安全 Agent 是系统的认知层。

它做三件事：

1. 读取当前事件和报警截图。
2. 查询历史记忆，判断该区域是否存在连续违规。
3. 调用 Qwen2.5-VL 进行视觉语义分析，并输出结构化建议。

输出内容包括：

- 态势概述。
- 建议风险等级。
- 判断依据。
- 建议工具动作。
- 是否需要人工确认。
- 置信度。

这部分解决的是“系统为什么认为这是风险”的解释性问题。

### 6.4 调度 Agent

调度 Agent 是系统的决策层。

它不会让 LLM 直接控制工具，而是采用“规则约束 + LLM 建议”的方式：

- 如果 LLM 试图降低高危等级，系统仍采用更保守的规则等级。
- 如果 LLM 建议升级且置信度足够，系统可以采纳升级建议。
- 最终执行动作必须来自规则表，避免 LLM 幻觉直接驱动设备。

当前规则：

| 等级 | 响应策略 |
|---|---|
| A | 高危拦截，生成审批工单，紧急推送，生成报告 |
| B | 自动放行，记录数据库，普通推送，记录日志 |
| C | 提示类事件，记录数据库 |

## 7. 工具与持久化

### 7.1 DatabaseTool

数据库文件：

```text
data/alarms.db
```

主要保存：

- `event_id`
- 事件时间
- 事件类型
- 风险等级
- bbox
- LLM 分析
- LLM 建议
- 调度裁决
- 工具执行结果
- 审批 ID
- 审批状态
- 执行 ID
- 执行状态
- 执行动作
- 生命周期状态
- 证据截图路径
- 时间线

### 7.2 HumanLoopTool

审批工单目录：

```text
data/pending/
```

A 类高危事件会生成：

```text
PENDING_yyyyMMdd_HHmmss_xxxxxx.json
```

工单包含：

- 工单 ID
- 事件 ID
- 事件类型
- 风险等级
- LLM 分析
- 调度裁决
- 当前状态
- 创建时间

### 7.3 ActuatorTool

执行记录目录：

```text
data/executions/
```

审批通过后生成：

```text
EXEC_yyyyMMdd_HHmmss_xxxxxx.json
```

当前执行器是工程模拟层，模拟三类处置：

- 声光报警联动。
- 高危区域设备联锁停机。
- 现场安全员处置工单派发。

这样做的意义是：在没有真实 PLC 或声光报警设备的情况下，系统仍然可以展示“审批之后到底执行了什么”，避免答辩时只有审批状态而没有执行闭环。

## 8. 前端系统详解

### 8.1 前端文件拆分

前端已经由单文件拆成三部分：

| 文件 | 职责 |
|---|---|
| `frontend/index.html` | 页面结构、模块顺序、importmap、脚本引用 |
| `frontend/css/twin.css` | 所有布局、颜色、面板、审批卡片、演示回放样式 |
| `frontend/js/app.js` | Three.js 场景、WebSocket、告警渲染、审批、演示触发 |

### 8.2 页面模块结构

前端页面主要模块：

```text
#top-bar        顶部标题、连接状态、Agent流程
#left-stack     左侧视频、趋势、健康面板
#right-panel    右侧可信闭环、AI分析、告警工单、演示回放
#view-controls  视角控制按钮
#status-core    场景核心状态提示
```

右侧面板当前顺序：

```text
系统标题
统计指标
可信闭环
AI视觉分析
告警/工单列表
演示回放
底部状态栏
```

### 8.3 3D 场景

前端使用 Three.js 构建 3D 数字孪生场景。

主要视觉对象：

- 工厂 GLB 模型。
- 合规工人 GLB 模型。
- 违规工人 GLB 模型。
- 地面网格。
- 危险区域。
- 数据流线。
- 光环和冲击波。
- CSS2D 悬浮标签。
- 告警目标框。

### 8.4 像素域到 3D 坐标映射

检测框来自摄像头像素域，前端使用 `p2w()` 将其映射到三维世界：

```text
bbox中心点 / 摄像头像素尺寸
    ↓
归一化坐标
    ↓
映射到 WORLD_W / WORLD_D
    ↓
3D工厂地面坐标
```

这样前端可以把真实检测到的人、车、火焰映射到数字孪生空间。

## 9. 前端模块位置调整指南

如果你后续想调整前端模块位置，主要看两个文件：

```text
frontend/index.html
frontend/css/twin.css
```

### 9.1 改模块顺序

模块 DOM 顺序在：

```text
frontend/index.html
```

常见位置：

- `#left-stack`：左侧模块顺序。
- `#right-panel`：右侧模块顺序。

如果你想让“演示回放”放到“AI视觉分析”上面或下面，就移动 `index.html` 中对应的：

```html
<div id="demo-panel">...</div>
```

如果你想让“告警列表”和“AI视觉分析”交换位置，就交换：

```html
<div id="llm-card">...</div>
<div id="alarms">...</div>
```

### 9.2 改整体左右宽度

在：

```text
frontend/css/twin.css
```

顶部 `:root` 里修改：

```css
--left-w:clamp(300px,21vw,360px);
--right-w:clamp(368px,25vw,430px);
--hud-gutter:clamp(16px,1.7vw,28px);
--side-top:calc(var(--top-height) + 26px);
--side-bottom:22px;
```

含义：

- `--left-w`：左侧面板宽度。
- `--right-w`：右侧面板宽度。
- `--hud-gutter`：左右面板离屏幕边缘距离。
- `--side-top`：左右面板顶部位置。
- `--side-bottom`：左右面板底部留白。

### 9.3 改左侧模块高度

位置：

```css
#left-stack{
  grid-template-rows:236px minmax(210px,1fr) 156px;
}
```

三行分别对应：

```text
实时视频
趋势图
系统健康
```

比如想让视频更高：

```css
grid-template-rows:270px minmax(190px,1fr) 156px;
```

### 9.4 改右侧模块高度

位置：

```css
#right-panel{
  grid-template-rows:auto auto minmax(148px,.46fr) minmax(92px,.24fr) minmax(220px,1fr) auto 34px;
}
```

当前对应：

```text
auto                 标题
auto                 统计指标
minmax(148px,.46fr)  可信闭环
minmax(92px,.24fr)   AI视觉分析
minmax(220px,1fr)    告警/工单列表
auto                 演示回放
34px                 底部状态栏
```

如果你觉得“AI视觉分析”太高，就调小第四项：

```css
minmax(78px,.18fr)
```

如果你觉得“告警/工单列表”太小，就调大第五项：

```css
minmax(260px,1fr)
```

### 9.5 改单个模块样式

常用选择器：

| 模块 | CSS选择器 |
|---|---|
| 顶部栏 | `#top-bar` |
| 左侧容器 | `#left-stack` |
| 右侧容器 | `#right-panel` |
| 实时视频 | `#cam-panel` |
| 趋势图 | `#chart-box` |
| 健康面板 | `#health` |
| 可信闭环 | `#trust-chain` |
| AI视觉分析 | `#llm-card` |
| 告警列表 | `#alarms` |
| 演示回放 | `#demo-panel` |
| 底部状态栏 | `#ft` |

### 9.6 改移动端布局

移动端和窄屏布局在文件底部：

```css
@media (max-width:1100px) { ... }
@media (max-width:900px) { ... }
@media (max-width:640px) { ... }
```

如果桌面端调好了，但窗口变窄又错位，需要同步改这些 `@media` 里的 `grid-template-rows`。

## 10. HTTP API

| 接口 | 方法 | 说明 |
|---|---|---|
| `/alarm` | POST | 接收真实检测报警 |
| `/demo/trigger` | POST | 触发演示回放 |
| `/approval/approve` | POST | 审批通过 |
| `/approval/reject` | POST | 审批驳回 |
| `/approval/pending` | GET | 查看待审批工单 |
| `/health` | GET | 查看系统健康状态 |
| `/camera/status` | GET | 摄像头状态 |
| `/camera/stream` | GET | MJPEG 实时视频 |
| `/recent_alarms` | GET | 最近事件 |
| `/latest_event` | GET | 最新事件 |
| `/latest.jpg` | GET | 最新截图 |
| `/alarms/{filename}` | GET | 证据截图 |

## 11. WebSocket 消息类型

前端主要处理三类消息：

| 类型 | 说明 |
|---|---|
| `alarm` | 基础告警，通常先到，用于快速更新3D场景和告警列表 |
| `alarm_with_llm` | LLM分析和调度结果，更新AI分析、可信链路和审批卡片 |
| `approval_result` | 审批和执行回写结果，更新审批状态、执行节点和生命周期 |

## 12. 启动方式

```powershell
cd D:\study\研电赛\digital_twin
python serve.py
```

启动后访问：

```text
http://localhost:8080
```

后端服务：

```text
http://localhost:5000
ws://localhost:5001
```

## 13. 推荐验证流程

### 13.1 验证前端是否正常

1. 启动 `python serve.py`。
2. 打开 `http://localhost:8080`。
3. 确认 3D 工厂出现。
4. 确认左侧实时视频有画面或显示摄像头状态。
5. 确认右侧可信闭环、AI分析、告警列表、演示回放都显示正常。

### 13.2 验证 A 类完整闭环

1. 点击“演示回放 → A 人车混行”。
2. 等待前端出现 A 类告警。
3. 等待 Agent 分析完成。
4. 查看审批卡片是否出现。
5. 点击“授权执行”。
6. 后端控制台应出现：

```text
[Actuator] EXEC_xxx event=EVT_xxx status=executed
[Approval] PENDING_xxx -> approved event=EVT_xxx db=True exec=executed
```

7. 前端可信闭环中“执行”节点应变为已执行。

### 13.3 验证 B/C 类

- B 类：点击“B 防护缺失”，应记录和推送，但不需要人工审批执行。
- C 类：点击“C 车辆检测”，应作为提示类事件记录。

## 14. 当前系统优势

从研电赛答辩角度，本系统的优势主要是：

- 不是纯前端展示，有真实后端和数据库。
- 不只是检测，有 Agent 分析和调度裁决。
- LLM 不只是生成文字，而是参与结构化风险建议。
- A 类事件有可信审批，避免自动执行高危动作。
- 审批之后有执行回写，闭环更完整。
- 前端 3D 可视化、实时视频、证据截图、可信链路统一展示。
- 演示回放保证现场稳定验证。
- 代码已经初步解耦，便于后续工程扩展。

## 15. 当前局限与后续方向

当前系统仍然是比赛原型，不是工业现场最终版。后续可以继续完善：

- 多摄像头管理和通道切换。
- 真实 PLC、声光报警器、门禁或停机设备联动。
- 审批用户身份认证和权限管理。
- 事件详情页和审计导出。
- 更标准的前端组件化，例如拆成 `ui/`、`scene/`、`api/` 多个 JS 模块。
- 后端配置文件化，把 RTSP、Webhook、模型名从代码移动到 `.env` 或 `config.yaml`。
- 自动化测试，包括演示回放测试、审批接口测试、数据库迁移测试。

## 16. 答辩时可以这样介绍

可以按下面逻辑讲：

```text
我们的系统不是简单报警大屏，而是一套端边协同的工厂安全数字孪生系统。

边缘侧负责目标检测，中心侧通过多智能体系统进行感知、认知和调度。
安全Agent结合历史记忆和Qwen2.5-VL进行风险分析，调度Agent在规则约束下采纳LLM建议。

对于A类高危事件，系统不会让AI直接执行处置动作，而是进入Human-in-the-Loop可信审批。
安全员在前端授权后，后端会更新审批状态，并通过执行器模块生成执行回写。

整个过程通过WebSocket实时同步到3D数字孪生前端，实现从检测、分析、裁决、审批到执行的可解释闭环。
```

这套表述能突出：

- 数字孪生。
- 多智能体。
- 大模型。
- 可信 AI。
- 人机协同。
- 工程闭环。
